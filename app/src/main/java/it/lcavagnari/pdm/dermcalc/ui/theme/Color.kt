package it.lcavagnari.pdm.dermcalc.ui.theme

import androidx.compose.ui.graphics.Color

/** Dark theme base colors for the Underground palette, used by Material surfaces and outlines. */

/** Dark-mode window background. */
val DarkBackground   = Color(0xFF0E0E10)
/** Dark-mode default Material surface. */
val DarkSurface      = Color(0xFF0E0E10)
/** Dark-mode low-emphasis surface variant for cards and bars. */
val DarkSurfaceLow   = Color(0xFF18181B)
/** Dark-mode high-emphasis surface for raised or active elements. */
val DarkSurfaceHigh  = Color(0xFF232327)
/** Dark-mode high-contrast outline for prominent borders. */
val DarkOutline      = Color(0xFFE8E6E0)
/** Dark-mode muted outline for secondary dividers and borders. */
val DarkOutlineDim   = Color(0xFF4A4A4F)
/** Dark-mode primary text and icon color on surfaces. */
val DarkOnSurface    = Color(0xFFF1EFE8)
/** Dark-mode subdued text and icon color on surfaces. */
val DarkOnSurfaceDim = Color(0xFF9C9A93)

/** Dark theme determination colors for primary actions and neutral chrome. */

/** Dark-mode Determination accent for primary UI emphasis. */
val Determination     = Color(0xFFE8C547)
/** Dark-mode Determination container for filled primary surfaces. */
val DeterminationCont = Color(0xFF3A2F00)
/** Dark-mode neutral UI accent for secondary controls. */
val NeutralUi         = Color(0xFFC9C7C0)

/** Light theme base colors for the Overworld palette, used by Material surfaces and outlines. */

/** Light-mode window background. */
val LightBackground   = Color(0xFFFAF8F2)
/** Light-mode default Material surface. */
val LightSurface      = Color(0xFFFFFFFF)
/** Light-mode low-emphasis surface variant for cards and bars. */
val LightSurfaceLow   = Color(0xFFF1EEE5)
/** Light-mode high-emphasis surface for raised or active elements. */
val LightSurfaceHigh  = Color(0xFFE5E2D7)
/** Light-mode high-contrast outline for prominent borders. */
val LightOutline      = Color(0xFF1A1A1B)
/** Light-mode muted outline for secondary dividers and borders. */
val LightOutlineDim   = Color(0xFFC9C6BB)
/** Light-mode primary text and icon color on surfaces. */
val LightOnSurface    = Color(0xFF1A1A1B)
/** Light-mode subdued text and icon color on surfaces. */
val LightOnSurfaceDim = Color(0xFF6A6862)

/** Light theme determination colors for primary actions and neutral chrome. */

/** Light-mode muted Determination accent for primary UI emphasis. */
val DeterminationMuted = Color(0xFF8C6B00)
/** Light-mode Determination container for filled primary surfaces. */
val DeterminationContL = Color(0xFFF5E9B8)
/** Light-mode neutral UI accent for secondary controls. */
val NeutralUiL         = Color(0xFF4A4842)
/** Light-mode red soul accent used where the full red would overpower the surface. */
val SoulRedMuted       = Color(0xFFB83838)

/** Severity colors encode clinical result tiers in dark and light themes. */

/** Dark-mode color for mild or normal severity badges. */
val SeverityMildDark      = Color(0xFF6BCB5C)
/** Dark-mode color for moderate severity badges. */
val SeverityModerateDark  = Color(0xFFF0A632)
/** Dark-mode color for severe severity badges. */
val SeveritySevereDark    = Color(0xFFE04848)
/** Light-mode color for mild or normal severity badges. */
val SeverityMildLight     = Color(0xFF2E8B3F)
/** Light-mode color for moderate severity badges. */
val SeverityModerateLight = Color(0xFFB86A18)
/** Light-mode color for severe severity badges. */
val SeveritySevereLight   = Color(0xFFB83838)

/** Soul colors identify routes, tools, borders, badges, and accent chrome across the app. */

/** Determination soul color for home and fallback route accents. */
val SoulDetermination = Color(0xFFE04848)
/** Patience soul color for BMI accents. */
val SoulPatience      = Color(0xFF4FB8B8)
/** Bravery soul color for BSA accents. */
val SoulBravery       = Color(0xFFE5853A)
/** Integrity soul color for PASI accents. */
val SoulIntegrity     = Color(0xFF314390)
/** Perseverance soul color for EASI accents. */
val SoulPerseverance  = Color(0xFF803CA1)
/** Kindness soul color for profile accents. */
val SoulKindness      = Color(0xFF5FB85F)
/** Justice soul color for tools and save-action accents. */
val SoulJustice       = Color(0xFFE8B842)

