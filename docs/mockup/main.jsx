import React from "react";
import ReactDOM from "react-dom/client";
import DermCalcMockup from "./DermCalcMockup2.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DermCalcMockup />
  </React.StrictMode>
);
